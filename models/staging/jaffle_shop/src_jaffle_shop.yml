version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null

      - name: orders
        loaded_at_field: _etl_loaded_at
        freshness: 
          warn_after: {count: 4, period: day}
          error_after: {count: 7, period: day}
        columns:
          - name: id
            tests:
              - unique
              - not_null
